    - name: Distribution
      debug: msg="{{ ansible_hostname }}"
      
    - name: copy dotfiles.tar to other systems
      copy:
        remote_src: false
        src: /home/mdries/dotfiles.tar
        dest: /tmp/ 

